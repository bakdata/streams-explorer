{{- if and .Values.serviceAccount.create (not .Values.serviceAccount.useExisting) -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  {{- if (not .Values.rbac.useExisting) }}
  name: streams-explorer-sa
  {{- else }}
  name: {{ .Values.serviceAccount.useExisting }}
  {{- end }}
{{- end -}}
