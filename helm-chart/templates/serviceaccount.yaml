{{- if and .Values.serviceAccount.create (not .Values.rbac.useExistingServiceAccount) -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "streams-explorer.serviceAccountName" . }}
{{- end -}}
